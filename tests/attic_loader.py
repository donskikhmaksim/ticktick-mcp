"""Загрузчик вынесенной автоуборки для тестов (2026-08-09, пункт 1.2.4).

ЗАЧЕМ. Пункт 1.2.4.г захода 1 закрывает дыру П6 физически: код автоуборки
(«разбор помойки») уехал из пакета в `attic/declutter_disabled.py` — каталог
без `__init__.py`, имя не является модулем `ticktick_mcp.src`, импортировать
его из сервера нечем. Вернуть автоуборку в строй раскомментированием больше
нельзя: надо осознанно перенести код обратно.

Но у этого кода 73 живых теста (test_declutter.py, test_declutter_sheet_
manifest.py и семь тестов в смешанных файлах), и все они обращаются к нему
как к `ticktick_mcp.src.server.<имя>` и патчат зависимости на модуле
`server` (`monkeypatch.setattr(s, "_ensure_ready", ...)` и ещё три десятка
имён). Выбросить их вместе с переносом — значит потерять покрытие; переписать
все обращения — значит поменять 55 тестов и потерять возможность механически
сверить, что поведение не поехало.

ЧТО ДЕЛАЕТ. Исполняет файл автоуборки в пространстве имён УЖЕ загруженного
модуля `server` (`exec(code, server.__dict__)`). После этого определения
автоуборки видны как `server._dc_*`, `server.plan_declutter` и т.д. — ровно
там, где их ищут тесты, и зависимости, которые тесты патчат на `server`,
видны коду автоуборки, потому что это буквально одни и те же глобалы.

ПОЧЕМУ ЭТО НЕ ВОЗВРАЩАЕТ ДЫРУ. Загрузчик живёт в `tests/` и вызывается
только из тестов. Боевой `server.py` его не импортирует, поэтому на живом
сервере файла автоуборки нет в процессе вообще. Плюс декораторы `@mcp.tool()`
у четырёх команд как были закомментированы, так и остались: даже под тестовым
загрузчиком команды в реестр MCP не попадают (`tools/tool_snapshot.py`
по-прежнему печатает 77) и запись в реестре исполнителей кнопки тоже
закомментирована и уехала в тот же файл.
"""
import pathlib

import ticktick_mcp.src.server as server

ATTIC = (pathlib.Path(__file__).resolve().parents[1]
         / "attic" / "declutter_disabled.py")

if not hasattr(server, "_dc_analyze"):        # идемпотентность: только раз
    _code = compile(ATTIC.read_text(encoding="utf-8"), str(ATTIC), "exec")
    exec(_code, server.__dict__)              # noqa: S102 — см. док-строку

# Удобный алиас: тесты, которым нужен именно модуль-донор, пишут
# `from tests.attic_loader import declutter as s`.
declutter = server
