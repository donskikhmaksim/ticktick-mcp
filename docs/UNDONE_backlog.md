# UNDONE backlog — почтовый пайплайн / критик / TickTick MCP

Консолидация «хотели, но не сделали» из трёх источников:
1. План `/Users/maksim/.claude/plans/joyful-whistling-flurry.md` (Фазы 0–9, дата 2026-07-27).
2. Транскрипты сессий («Tg AI ASSISTANT» `local_e8632d51…`, «Поиск помещения для Fix&Roll» `local_8275dddd…`, «Telegram bot diagnostics» `local_fcd31974…`, «TickTick MCP» `local_141353e0…`).
3. Живой код: `~/Code/n8n-railway/workflows/email-pipeline.json` + `docs/DESIGN_n8n_to_shim.md`, `~/Code/claude-p-shim`, `~/Code/tg-ai-assistant` (docs/FEATURES.md), `~/Code/ticktick-mcp/docs/DESIGN_*.md`.

Только сбор — ничего не реализовано, ничего в коде/TickTick не тронуто.

---

## Группа A — n8n-пайплайн / критик писем (`n8n-railway`, `email-pipeline.json`)

### A1. Критик → pure-text через claude-p-shim (Фаза 3 плана)
- **Хотели:** убрать из workflow тройку `AI Agent + Anthropic Chat Model + TickTick MCP(tool)` (это платный Anthropic API + tool-calling), заменить одним узлом `HTTP Request` в **расширенный** claude-p-shim (шим сам ведёт agentic-цикл через `claude -p --mcp-config` к TickTick MCP). Формат ответа критика (`РЕШЕНИЕ:`/`ОТВЕТ_НУЖЕН:`/`ДОСТУП_ПОДТВЕРДИТЬ:`) не меняется.
- **Статус:** **спроектировано, не реализовано.** Подтверждено живым кодом: `email-pipeline.json` сейчас содержит именно старую тройку узлов (`AI Agent`, `Anthropic Chat Model` — платный Anthropic-ключ, `TickTick MCP` как ai_tool). Дизайн-документ `~/Code/n8n-railway/docs/DESIGN_n8n_to_shim.md` даёт полный план (расширить шим `--mcp-config`, отдельный `SHIM_MCP_TOKEN`, переставить workflow), но шим (`~/Code/claude-p-shim/shim.py`) до сих пор **чистый text-in/text-out**, без MCP-инструментов.
- **Сложность:** средне-сложно (два репо: shim + n8n workflow; нужна ручная проверка `--mcp-config` в неинтерактивном режиме перед переносом).
- **Риск для живого пайплайна:** средний — меняет исполнителя критика (agent loop уходит из n8n внутрь shim), нужен staging-прогон на 1-2 письмах перед вкл. в проде; неверный timeout/approval-режим может тихо не создавать задачи.
- **Ценность:** высокая — снимает оплату Anthropic API за каждое письмо, экономика пайплайна.

### A2. Security-кнопки / гейт `ДОСТУП_ПОДТВЕРДИТЬ` (Фаза 4 плана)
- **Хотели:** воркфлоу sec-confirm — по вердикту критика `ДОСТУП_ПОДТВЕРДИТЬ=да` слать в Telegram сообщение с URL-кнопками («Подтвердить» → архив+ack TG; «Отклонить» → тревога + ссылка безопасности Google), не через callback, а через настоящие URL-кнопки.
- **Статус:** **не начато.** Критик уже сейчас ПИШЕТ это поле в текстовом вердикте (промпт явно требует `ДОСТУП_ПОДТВЕРДИТЬ: <да|нет> | <приложение или ->`), но в workflow нет ни одного узла, который это поле читает/маршрутизирует — данные вычисляются и никуда не используются. Webhook/кнопки отсутствуют.
- **Сложность:** средне (нужен Telegram webhook GET-эндпоинт, новый узел-роутер по полю).
- **Риск:** низкий сам по себе, но это дыра безопасности сейчас — потенциальный сигнал «нужно подтвердить доступ» просто теряется, никуда не долетает.
- **Ценность:** высокая с точки зрения безопасности — единственный из недоделанных пунктов, который явно про защиту аккаунта.

### A3. Лог улучшений критика → TickTick (Фаза 5 плана)
- **Хотели:** если финальное решение критика расходится с алгоритмом (`final≠algo`) или `confidence=low` — заводить задачу-предложение в TickTick с ежедневной ревизией расхождений.
- **Статус:** не начато. В коде нет узла, который бы сравнивал algo vs final и создавал flag-задачу.
- **Сложность:** просто-средне (нужно только сравнение полей + create_tasks вызов).
- **Риск:** низкий (не мутирует почту, только TickTick).
- **Ценность:** средняя — это единственный механизм самообучения/контроля качества критика, сейчас расхождения молча теряются.

### A4. Покрытие TRASH_SENDERS + обновление скилла ticktick-daily-review (Фаза 6 плана)
- **Хотели:** дозаполнить список `TRASH_SENDERS` по скилловой методичке «AUTO-УДАЛЕНИЕ», передать список критику; обновить скилл `ticktick-daily-review` одной версией (сверка mail_inbox + ревизия улучшений + накопленные правки).
- **Статус:** не начато. Строка/переменная `TRASH_SENDERS` не встречается ни в `n8n-railway`, ни в `tg-ai-assistant`.
- **Сложность:** просто (список email-адресов + правка промпта критика).
- **Риск:** низкий, но без него часть спама/рассылок продолжает доходить до критика вместо автоочистки.
- **Ценность:** средняя, экономит вызовы критика/шима на явный мусор.

### A5. Feature-flags + auto-updater (Фаза 7 плана)
- **Хотели:** конфиг фичефлагов для пайплайна + auto-updater (через Claude Code cron), но **только после одобрения Максима**, с батч-тестом перед деплоем.
- **Статус:** не начато (и явно не приоритет — сам план ставит approval-гейт перед началом). В соседнем проекте `tg-ai-assistant` уже есть похожая, но отдельная тема: auto-updater туда **отвергнут** («no third option — нельзя пушить auto-updater на бокс без inbound update-канала», см. сессию Tg AI ASSISTANT) — стоит учитывать этот прецедент, если фича всплывёт снова для n8n-пайплайна.
- **Сложность:** сложно (auto-updater), просто (сами флаги).
- **Риск:** auto-updater — высокий риск при плохой реализации (могло сломать прод без надзора); поэтому и был gate на одобрение.
- **Ценность:** низкая-средняя, инфраструктурная гигиена, не блокер для остального.

### A6. Живой тест reply→TG (Фаза 8 плана)
- **Хотели:** проверить на живом письме «нужен ответ», что TG-пинг реально приходит.
- **Статус:** **частично** — код-путь уже существует в живом workflow (`Нужен ответ?` → `TG уведомление`, оба узла присутствуют в `email-pipeline.json`), но по плану это ещё не прогнано на живом тестовом письме («живое письмо → TG-пинг» — verification-пункт).
- **Сложность:** просто (это тест, не разработка).
- **Риск:** низкий.
- **Ценность:** средняя — нужно для доверия к остальной цепочке, дешёвая проверка.

### A7. Календарные приглашения из пайплайна (Фаза 9 плана — самая крупная недоделка)
- **Хотели:** когда критик распознаёт письмо-запись/приём — не писать прямо в календарь Максима, а слать НАСТОЯЩЕЕ календарное приглашение с отдельного сервисного Gmail-аккаунта (`attendee=donskikh.ms@gmail.com`, `sendUpdates=all`), с двухслойным дедупом (код: Calendar-узел ищет события ±1 день; LLM/шим: семантическая сверка «это то же событие?»), и тремя исходами (ничего/обогатить существующее/создать новое). Плюс применить правило «−10/15/20 мин молча».
- **Статус:** **не начато совсем.** Ни в `n8n-railway`, ни где-либо ещё нет упоминаний `calendar_event_create`/`sendUpdates`/`attendee`-логики. Пререквизит (отдельный Gmail-аккаунт под сервисный OAuth-клиент) — по плану создаёт сам Максим, тоже не подтверждено сделанным.
- **Сложность:** сложно — новый OAuth-клиент, новый n8n Calendar-узел, двухслойный дедуп, интеграция с шимом для семантической сверки.
- **Риск:** средний-высокий для живого пайплайна, если делать в общем workflow (создание дублей событий в личном календаре, если дедуп ошибётся) — но изолируемо как отдельная ветка/subworkflow, что снижает риск для остального критика.
- **Ценность:** высокая по эргономике (нативный Accept/Decline вместо отдельной TickTick-задачи), но это **самая объёмная** отдельная фича из всего списка — кандидат на отдельный спринт, не «доделать между делом».

---

## Группа B — ticktick-mcp (спроектировано в `docs/DESIGN_*.md`, не реализовано)

Все шесть DESIGN-документов имеют статус **«спека для реализации / только проектирование — код не менялся»** явным текстом в самом файле. Ни один из них не воплощён в `server.py` (это отдельная параллельная ветка задачи #29, не входящая напрямую в план Фаз 0-9, но тот же контур ценности «критик/TickTick»).

### B1. Sheet-backed манифест для declutter (`DESIGN_sheet_backed_declutter.md`)
- **Хотели:** состояние declutter-разбора (что удалить/переименовать/сгруппировать) — не в RAM (TTL 1ч, гибнет при рестарте), а в Google Sheet: durable, resume, живой аудит одобрений человеком по колонке `decision`.
- **Статус:** спроектировано полностью (схема листа, API `plan_declutter(persist="sheet")`/`execute_declutter`/`resume_declutter`/`set_declutter_decision`, идемпотентность, гонки одобрения). Не реализовано — `declutter_sheet.py` в коде уже существует как файл (проверено `ls`), но независимая **проверка на живых кредах** (`DECLUTTER_SHEET_ID`/`GSHEETS_SA_JSON` в Railway env) по свежему DESIGN_aggressive_declutter... документу **отсутствует** — это явный блокер №1 для дальнейшего тюнинга.
- **Сложность:** средне (модуль уже частично есть, нужно поднять env + протестировать).
- **Риск:** низкий, opt-in флагом, не трогает текущий RAM-путь.
- **Ценность:** высокая — без него весь агрессивный семантический declutter (B-пункт ниже) невозможно итеративно тюнить.

### B2. Агрессивный многоролевой declutter (`DESIGN_aggressive_declutter_reclassify.md`)
- **Хотели:** заменить лексический Jaccard-кластеризатор (сейчас ловит 8 задач из 209 у Максима) на 5 LLM-ролей (судья дублей, smart-переформулировщик, критик нужности, классификатор колонок, umbrella-группировщик) с реалистичной оценкой «≥50 задач к удалению/переименованию» на проекте Fix&Roll.
- **Статус:** спроектировано детально (промпты ролей, таксономия из 7 колонок, ручки агрессивности), **не реализовано**. Явно зависит от B1 (Sheet-persist) как пререквизита.
- **Сложность:** сложно (5 новых ролей + новый action-тип `reclass`/`flag_junk` в Sheet-схеме).
- **Риск:** средний — работает на реальном проекте Fix&Roll с боевыми задачами; смягчается тем, что удаление всегда только через explicit-approve человека.
- **Ценность:** высокая лично для Максима (реальная помойка ≥150 задач подтверждена), но объёмная работа.

### B3. Настоящий гейт подтверждения (`DESIGN_approval_gate.md`)
- **Хотели:** заменить нынешний «фейковый» `confirm="DELETE N"` (модель сама печатает и сама же может ввести — не доказательство согласия человека) на Telegram-push одноразового nonce (или Г2: inline-кнопка → серверный флаг), привязанного к манифесту+объектам, с TTL и лок-аутом.
- **Статус:** спроектировано (модель угроз, варианты А1-А4/Б/В/Г1-Г3, рекомендация А1+Б, миграция на Г2), **не реализовано**. Это касается **всех** мутирующих write-инструментов TickTick MCP, включая `execute_task_deletion`, `execute_declutter`, `delete_project`, и особенно дыру `set_declutter_decision` (сейчас модель может сама поставить `approved` без всякой проверки).
- **Сложность:** средне-сложно (нужен `TG_BOT_TOKEN`/`TG_OWNER_CHAT_ID`, единая точка `_require_consent()`).
- **Риск:** это САМ по себе security-фикс — риск не в реализации, а в том, что **сейчас** его отсутствие означает, что модель теоретически может «подтвердить сама себя» на удаление задач/проектов.
- **Ценность:** высокая — это единственный пункт из всего списка, который напрямую закрывает реальную дыру согласия (не «удобство», а «модель не должна уметь удалить без человека»).

### B4. Единый формат ответа write-инструментов (`DESIGN_output_format.md`)
- **Хотели:** один markdown-шаблон ответа (заголовок+тело+пруф) для всех ~24 пишущих инструментов TickTick MCP + серверная принудительная склейка пруфа (уже частично есть в `execute_task_creation`) — против «пусто в чате», когда модель не перепечатывает результат.
- **Статус:** спроектировано (эмодзи-легенда, группы A/B/C инструментов, честный потолок доставки без клиентского виджета), не реализовано.
- **Сложность:** просто-средне (в основном рефакторинг текстовых шаблонов, без новой логики).
- **Риск:** низкий, чисто косметическая/UX правка.
- **Ценность:** средняя — не блокирует функциональность, но снижает «пусто в чате» и разнобой EN/RU в ответах (`create_project`, `checkin_habit` сейчас на английском).

### B5. `assign_owners` — назначение владельцев бесхозным задачам (`DESIGN_assign_owners.md`)
- **Хотели:** отдельный инструмент (не часть declutter) для shared-проектов: находит задачи без `assignee`, предлагает владельца (правило + shim-судья), человек одобряет в Sheet, код проставляет через уже-аудированный `update_tasks`.
- **Статус:** спроектировано полностью, не реализовано. Переиспользует Sheet-механизм из B1 (отдельная вкладка `Assign Log`).
- **Сложность:** средне (в основном копирование паттерна declutter, но с другим дефолтом `decision=pending` для каждой строки).
- **Риск:** низкий-средний (неверный assignee — не катастрофа, легко переназначить; но сам механизм не журналируется TickTick-ом нативно).
- **Ценность:** средняя — полезно, но нишевая фича относительно основного пайплайна.

### B6. Таксономия write-инструментов + гейт-рекомендации (`DESIGN_write_tool_taxonomy.md`)
- **Хотели:** классификация всех 35 пишущих инструментов по силе нужного гейта (🔴/🟡/🟢) + список из 3 явно ЛГУЩИХ докстрингов (`plan_task_deletion` врёт, что прямое удаление отключено; `delete_task_with_subtasks` описывает несуществующее поведение; `set_declutter_decision` вводит в заблуждение про «approval path», хотя проверки согласия нет).
- **Статус:** сам документ — завершённый анализ (не «недоделка» как таковая), но **рекомендации из него не внедрены** — докстринги до сих пор лгут, `_require_consent()` (из B3) до сих пор не существует.
- **Сложность:** просто (правка текста докстрингов) для честности; средне-сложно для полного внедрения гейт-рубрики (зависит от B3).
- **Риск:** сама ложь в докстринге — риск для будущих агентов/Максима, которые доверятся неверному описанию поведения.
- **Ценность:** высокая по цене/эффекту — исправление 3 докстрингов почти бесплатно устраняет реальную путаницу про безопасность прямо сейчас, без ожидания полного B3.

---

## Группа C — прочее / сквозное

### C1. Watchdog/healthcheck пайплайна не влит в main (`tg-ai-assistant`)
- **Хотели (Фаза 0 контекст + docs/FEATURES.md):** ежедневный сторож (`app/pipeline/watchdog.py`, `claude.healthcheck()`/`qwen.healthcheck()`, cron-джоба), который пингует всю цепочку извлечения и предупреждает владельца в личку, если shim/tier-2 отвалился (мотивация — реальный инцидент: разлогиненный `claude` на Mac mini молча стопорил создание задач днями).
- **Статус:** **частично** — код существует в ветке `feat/extraction-watchdog`, но **не влит в `main`** (подтверждено `docs/FEATURES.md` того репо, раздел 3).
- **Сложность:** просто (код уже написан, нужен только merge + деплой).
- **Риск:** низкий, watchdog — чисто наблюдательный код.
- **Ценность:** высокая относительно затрат — уже написан, просто лежит смерженным; самый дёшево доделываемый пункт всего списка.

### C2. Мета-скилл `skill-updating` + добавление скилла `task-intake` (Фаза 0 плана)
- **Хотели:** создать и загрузить в claude.ai Skills мета-правило «как обновлять скиллы/инструкции из Claude Code/Cowork» (3 сценария: скиллы через Downloads+Add, инструкции General/Cowork через бэкап+пересборка+вставка+перечитывание, Drive-скиллы через `skill_version_update`), плюс добавить сам скилл `task-intake` в интерфейс claude.ai.
- **Статус:** не подтверждено сделанным (план явно формулирует это как ещё не выполненный хвост Фазы A/0); файл `scratchpad/task-intake-SKILL.md` упомянут как заготовка, но её загрузка в claude.ai — ручной шаг, не автоматизируемый и не проверяемый из кода.
- **Сложность:** просто, но ручной (нужны действия Максима в браузере claude.ai).
- **Риск:** нет.
- **Ценность:** низкая-средняя, организационная гигиена.

---

## Итог (что доделать первым)

**Топ по группам:**
- **n8n-критик:** A2 (security-кнопки — единственная реальная брешь безопасности, данные уже вычисляются критиком и никуда не идут) → A1 (shim-перевод, экономит деньги, но рискует стабильностью — нужен staging) → A7 (календарные приглашения — самая ценная фича по удобству, но и самая объёмная, отдельный спринт) → A3/A4/A6 (мелкие, дёшевые).
- **ticktick-mcp:** B3 (настоящий гейт согласия) и B6 (исправить 3 лгущих докстринга — почти бесплатно) — это реальная защита от «модель сама себя подтвердила»; B1+B2 (Sheet-declutter) ценны лично для Максима, но объёмны и не блокируют пайплайн; B4/B5 — косметика/нишевое.
- **прочее:** C1 (watchdog-merge) — самое дёшевое из всего списка, код уже готов, просто не влит.

**Самое ценное доделать первым** (риск/цена/выгода): (1) **A2** — security-кнопки, потому что сигнал угрозы аккаунта критик уже генерирует, но он никуда не доходит; (2) **B6-докстринги** — 15 минут правки текста против реальной путаницы про то, что удаление «отключено»; (3) **C1** — влить готовый watchdog, ноль новой разработки; затем уже — A1 (shim) и B3 (гейт) как более объёмные, но структурные фиксы.
