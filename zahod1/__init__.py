"""Папка сдачи захода 1.

Не входит в состав команд сервера и ни одной строки в коде сервера не трогает.
Разрешена заданием явно: docs/TZ/ZAHOD1.md, раздел 2.3, строка «Прогон — одна
команда, `python -m zahod1.check --dsn "$TICKTICK_TEST_PG_DSN"`. Живёт в
отдельной папке `zahod1/`».
"""
